package com.belote.entity;

public enum TournamentStatus {
    REGISTRATION,    // When teams can register
    IN_PROGRESS,     // When matches are being played
    COMPLETED        // When tournament is finished
}
